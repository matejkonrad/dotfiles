return {
  "akinsho/bufferline.nvim",
  opts = {
    options = {
      -- Show the buffer line as soon as there's one buffer. LazyVim defaults
      -- this to false, which hides the line until you have 2+ buffers.
      always_show_bufferline = true,
    },
  },
}
