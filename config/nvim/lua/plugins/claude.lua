return {
  "coder/claudecode.nvim",
  opts = {
    auto_start = true,
    terminal = {
      provider = "none",
    },
    diff_opts = {
      open_in_new_tab = false,
      layout = "vertical",
    },
  },
}
